with Ada.Text_IO; use Ada.Text_IO;

procedure Ejemplo_Stack is
   -- Declaración de una variable local
   Numero : Integer := 10;

begin
   -- Imprimir el valor de la variable local
   Put_Line("El valor de la variable en la stack es: " & Integer'Image(Numero));
end Ejemplo_Stack;
